; PHP limits (cPanel / PHP-FPM — copy to site root if .htaccess php_value is ignored)
upload_max_filesize = 64M
post_max_size = 64M
max_file_uploads = 25
max_input_time = 300
max_execution_time = 300
